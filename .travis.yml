language: java
jdk: openjdk8
services:
  - xvfb    

before_install:
  - sudo apt update
  - sudo apt install openjfx
  - export DISPLAY=:99.0

install: true
script: 
 - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V --file Wahlverteiler/pom.xml
 - mvn test -B --file Wahlverteiler/pom.xml
